rpl_interface_timing = {$time} ns has been used during `do_match` after checking {$crate_name}
    .note = this is the elapsed time for graph matching
    .note = you're seeing this because the `timing` feature is enabled
